# 更新日志

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 风格，版本号规则见 [VERSIONING.md](./VERSIONING.md)。

## [Unreleased]

## [1.5.1] - 2026-08-20

### 安全

- **修复剪藏正文 HTML 清洗的协议绕过 XSS**：`sanitizeHtml` 原先仅用 `new URL(href, base)` 解析链接 / 图片地址后原样写回，但 `new URL('javascript:alert(1)', base)` 不会抛错且会保留 `javascript:` 协议——剪藏正文里混入 `<a href="javascript:...">` 或 `data:text/html,<script>` 的恶意链接，用户在阅读视图点击即可执行任意脚本（XSS）。现按协议白名单放行（链接仅 `http(s)` / `mailto` / `tel`，图片仅 `http(s)` / `data:image/*`），危险协议删除 `href`、保留可见文本，无 `src` 的图片残骸移除。
- **服务端改为仅绑定回环地址**：该工具无身份认证，且 `/api/summarize` 与 `/api/test-provider` 会按请求体里的 `baseUrl` 让本服务发起任意 POST（SSRF 原语）。原先 `app.listen(PORT)` 默认绑定 `0.0.0.0`，同网段任意主机可读写整个剪藏库并借本服务探测内网。现显式绑定 `127.0.0.1`，仅本机可访问。如确需局域网访问，请在反向代理层加鉴权后再暴露。

### 内部

- 删除 `src/llm.js` 中重复定义的 `flattenModels`（两份完全相同的函数声明，靠函数提升使后者生效），保留单一定义。

## [1.5.0] - 2026-08-14

### 新增

- **添加剪藏交互重构**：「生成摘要」按钮改为「解析」，解析完成后按钮消失；未保存的解析结果在重新打开浮窗时直接回显（URL 一并回填），可继续编辑标签或直接保存；结果卡新增「清除」按钮，二次确认后才丢弃，防误触丢失

## [1.4.0] - 2026-08-14

### 新增

- **剪藏库多维筛选**：左栏新增「平台」筛选；右栏标签 / 作者改为高亮选中 + 筛选卡片（带关闭按钮）的交互；「未知作者 / 未知平台」可正确筛出对应文章（原先搜索框 LIKE 匹配不到）
- **排序方向切换**：列表 API 新增 `order=asc|desc`；「最近保存 / Token 用量 / 费用」改为搜索行右侧的滑块式分段控件，支持正序 / 倒序一键切换
- **设置页改版**：中英切换从顶栏移入设置页；「解析模式」改为滑块式分段控件，与界面语言同款行式布局
- README 拆分为中英双语（`README.md` / `README.zh.md`），补全 GitHub About 与 Topics

### 变更

- 统一界面元素圆角（6px 外 / 4px 内）：排序滑块、中英切换、服务商开关
- 搜索框、排序滑块、刷新按钮统一为 38px 高度
- 用量趋势图固定显示最近 30 天（窄页面自动切 7 天），柱宽均一

### 修复

- 用量趋势图时区 bug：东八区下日期序列被 UTC 前移一天，当天数据恒为 0；同类问题一并修复剪藏库「按周 / 按天」时间筛选的边界计算
- 「最近保存」滑块在从其他页签进入剪藏库时偶发丢失绿色背景（隐藏面板下 `offsetWidth` 为 0，切换页签后重新校准）
- 作者排行「未知」选中后无结果（改为独立作者筛选参数，`__none__` 匹配无作者剪藏）

## [1.3.0] - 2026-08-14

### 新增

- **正文图片本地化**：解析时自动下载远程图片到 `data/images/` 并将 `src` 改写为本地路径（`/images/...`），解决微信等防盗链站点图片无法显示的问题；并发下载（4 路、单图 8s 超时），按 URL 哈希去重，失败的图片保留远程地址不阻塞主流程；已本地化的路径在重新解析 / 编辑保存时保持不变

## [1.2.0] - 2026-08-14

### 新增

- **AI 辅助解析模式**：设置中新增解析模式（js 规则解析 / AI 辅助大纲提取）；`parseMode=ai` 时服务端调用 LLM 补全大纲，失败自动回退 js 解析；LLM 调用重构为通用 chat completion，复用于摘要与大纲提取
- **图片懒加载支持**：`data-src` 回填为 `src`，清理无 `src` 的残留图片

### 变更

- 目录展示阈值调整并保留左栏占位，避免三栏布局错位
- 统一文章排版并清理残留空元素

### 修复

- **混合标题大纲提取**：合并大纲提取逻辑，覆盖 H1–H4 混合出现的场景

## [1.1.0] - 2026-08-13

### 新增

- **阅读高亮与评论**：阅读正文时选中文字即可加高亮并附评论；左栏支持「目录 / 高亮」分页切换，展示本篇全部高亮；剪藏库与首页列表显示高亮情况
  - 定位不依赖 DOM 偏移：高亮以「精确文本 + 前后上下文」存储，还原时按文本匹配重新包裹，文章被编辑后仍可恢复
  - 评论支持保存、编辑、删除
- **多颜色高亮**：黄 / 蓝 / 红三色，选中文字时浮现色块，点选即用该色创建高亮
  - 已有高亮可在评论浮窗内即时改色，与评论互不影响
- **按色统计**：首页与剪藏库列表项以「色点 + 数字」分别显示三色高亮数

### 修复

- **高亮跨内联标签无法显示**：选中文字跨越 `<a>`/`<strong>` 等内联标签边界时，原实现会在正文静默失效；改为逐文本节点 `splitText` 包裹（标签式），并修正了会多包裹上下文文本的偏移 bug
- **评论弹窗按钮对齐**：修正弹窗内「删除 / 取消 / 保存」按钮因 `.btn-primary` 全局 `margin-top` 导致的错位，统一高度与基线

## [1.0.0] - 2026-08-12

首个正式版本。以下按日期记录的开发历史均归入 1.0.0。

## 2026-08-12

### 新增：项目添加 MIT 许可证

- **LICENSE 文件创建**：在项目根目录新增标准 MIT 许可证文件，版权归属于 CTZ（2026 年）
- **package.json 已对齐**：`package.json` 中 `license` 字段已设置为 `"MIT"`，与 LICENSE 文件保持一致
- **修改建议背景**：开源项目缺少明确许可证会导致他人使用、修改、分发时存在法律风险。MIT 许可证宽松简洁，允许他人自由使用、复制、修改、合并、发布、分发、再许可，仅需保留版权声明与许可声明，适合个人开源项目

## 2026-08-11

### 调整：首页顶部信息区排版优化

- **一句话总结上移**：移除「近期在看」标题，将一句话总结从热力图右侧移至热力图正上方；顶部行由三栏（计数/热力图/一句话）改为两栏（计数 / 一句话叠热力图）
- **热力图去框**：移除贡献热力图的外框、背景与阴影，直接融入页面底色
- **热力图悬停提示**：格子的原生 `title` 提示改为跟随鼠标的自定义深色浮层，悬停即显示日期与篇数（如「2026-08-11：3 篇」）；事件委托绑定一次，避免重复监听
- **标签云去框**：移除标签云卡片外框与背景，仅保留「标签云」标签与浮动标签
- **标签云上移**：标签云从底部「最近剪藏」旁上移至顶部行，显示在热力图右侧；顶部行恢复三栏（计数 / 一句话叠热力图 / 标签云，190px · 1fr · 280px）；底部「最近剪藏」改为全宽
- **一句话总结去背景**：移除暖色背景与左侧边框，仅保留衬线字体与文字色，融入页面底色（阅读页编辑态仍保留高亮背景以提示可编辑）
- **Tab 自适应宽度**：顶部三个 Tab 由均分全宽改为内容自适应宽度、左对齐排列（`flex: none` + `gap: 24px`）
- **设置页适配宽屏**：用量统计数字（剪藏总数/累计 Token/累计费用）从下方移至「服务商配置」右侧，主区改为两栏布局（1fr · 320px），窄屏自动堆叠并恢复横向统计条；左右两栏等高（`align-items: stretch`），服务商配置卡片高度至少与右侧统计数字列一致
- **用量趋势改柱状图**：趋势图由折线图改为柱状图（`fillRect`），高度从 280px 降至 180px；柱宽自适应、柱顶按间隔显示数值；标题由「Token 用量趋势」简化为「用量趋势」

### 重构：作者字段改为列表，统计时拆分

将 `author` 从单字符串改为 JSON 数组存储，一篇文章可关联多个作者；统计时按作者展开独立计数。

- **多作者提取**：抓取网页时用 `querySelectorAll` 收集所有 `article:author`/`author` meta 标签、解析 JSON-LD `author` 数组、对含分隔符（逗号/顿号/分号/`&`/和/与/and）的值拆分，去重后存为数组
- **统计拆分**：`byAuthor` 由 `GROUP BY author` 整串聚合改为 JS 层展开数组聚合（与 `topTags` 同风格），每个作者独立计数；空作者归入「未知」；新增 `totalTokens`/`totalCost` 维度，共同署名各享全文 token/费用
- **历史数据迁移**：启动时幂等迁移旧单字符串——按分隔符拆分为数组，已是数组的跳过
- **编辑支持**：`updateClipping` 与 PUT 路由白名单加入 `author`，可在编辑页修改作者
- **前端适配**：列表/详情/首页/摘要结果 4 处作者展示改为 `join('、')`；作者排行与统计页 `renderDist` 通用渲染无需改动
- **搜索不变**：`author LIKE @q` 对 JSON 字符串子串匹配，效果等同按作者名模糊搜索（与 `tags LIKE` 同策略）

### 重构：全宽布局与信息架构升级

将整体从窄居中布局改为全宽布局，重构首页/剪藏库/设置三个页面，解决「文章打开后占满屏幕、打开前却只在中间显示」的割裂感。

- **全宽容器**：`.container` 提升至 `max-width: 1400px`，首页/剪藏库/设置页分别按宽度重新排布；窄屏（≤1100px）自动堆叠为单列
- **品牌区**：标题改为 `CTZ's Web Summary Index`，与副标题「抓取网页 → AI 中文摘要 → 永久收藏与统计」一起左上角显示；右上角放置「+ 添加剪藏」按钮
- **顶部 Tab 精简**：由 4 个 Tab（生成摘要/剪藏库/统计/设置）合并为 3 个（首页/剪藏库/设置）；「生成摘要」入口改为浮窗，「统计」并入「设置」

### 新增：首页仪表盘

- **顶部行**：三张计数卡（条目数/标签量/Token 消耗）+ 贡献热力图（近 365 天，5 级森林绿色阶）+ 「近期在看」一句话总结（前端模板聚合近 7 天篇数与高频标签，零 LLM）
- **标签云**：最近剪藏旁新增频次标签云，标签出现次数越多字号越大（12–26px 线性映射），点击跳转剪藏库并按该标签筛选
- **添加剪藏浮窗**：点击「+ 添加剪藏」弹出原「生成摘要」页面（URL 输入 + 服务商/模型选择 + 结果展示），保存后自动关闭并刷新首页/剪藏库

### 新增：剪藏库三栏布局

- **左栏（时间聚类筛选）**：按年/月/周/日聚类，显示对应时间范围内的笔记数；点击即筛选，上方出现时间筛选徽章（可一键清除）
- **中栏**：搜索框 + 标签/排序工具条 + 剪藏列表
- **右栏（排行）**：标签排行 + 作者排行，点击标签按标签筛选、点击作者按作者搜索

### 变更：设置页合并统计

- 「设置」页在「服务商配置」下方并入原统计页内容：累计数字卡（剪藏总数/累计 Token/累计费用）、Token 用量趋势图（Token/费用/剪藏数三指标切换）、按模型/平台/作者分布

### 技术说明（全宽重构）

- **后端**：`db.js` 新增 `getTimeClusters()`（年/月/周/日聚合）、`getStats()` 增加 `byAuthor` 与 `distinctTags`；`listClippings()` 支持 `from`/`to` 日期范围过滤；`router/stats.js` 新增 `GET /api/stats/clusters`
- **周聚类稳定性**：周分组以「所在周周一日期」为 key（`date(saved_at, '-' || ((CAST(strftime('%w',saved_at) AS INT)+6)%7) || ' days')`），而非 SQLite `%W` 字符串，避免前端换算 from/to 时的周界错乱
- **前端**：`app.js` 新增 `renderHomeTagCloud` / `renderTimeClusters` / `applyTimeFilter` / `renderLibraryRails` / 浮窗开关；`loadLibrary` 并发拉取列表+统计+聚类；ESC 键扩展为依次关闭阅读页→添加浮窗→服务商浮窗
- **安全**：时间筛选 `from`/`to` 走 `listClippings` 参数化查询，无注入风险；标签云/排行经 `escapeHtml` 转义

## 2026-08-11（阅读视图与编辑）

### 新增：三栏阅读视图

将文章详情从弹窗升级为全屏三栏阅读视图，提升阅读体验。

- **左栏（目录）**：从全文 HTML 的 H1-H4 标题自动生成可点击目录，点击平滑滚动到对应标题；旧剪藏无 H 标签时降级为静态目录展示
- **中栏（全文）**：渲染后端清洗后的结构化全文 HTML（支持图片、代码块、表格、引用等富文本）；旧剪藏无 contentHtml 时降级为纯文本展示并显示提示
- **右栏（摘要信息）**：一句话总结、摘要、标签编辑、模型与用量、原文链接

### 修复

- `handleSave()` 保存时缺失 `contentText` 和 `contentHtml` 字段，导致已保存剪藏无法在阅读页显示全文（BUG 修复）

### 变更

- `openDetail(id)` 从打开弹窗改为打开全屏阅读视图
- 文章元信息（平台/作者/发布时间/收藏时间）从阅读页头部迁移到右栏顶部「文章信息」区块，显示在一句话总结前面，每项独立一行
- 标签编辑功能从弹窗迁移到阅读页右栏
- 删除操作从弹窗内按钮迁移到阅读页头部
- 新增 ESC 键关闭阅读页
- 移除已废弃的 `#detailModal` 弹窗结构及关联的 `closeModal()`、`renderModalTags()` 函数

### 技术说明

- 仅改动前端（`public/app.js`、`public/style.css`、`public/index.html`），后端无需改动
- 后端 `extract.js` 的 `sanitizeHtml()` 已对全文 HTML 做白名单过滤，前端直接 `innerHTML` 渲染安全
- 后端 `sanitizeHtml()` 已为 H 标签添加 id，前端 TOC 锚点跳转基于此 id

### 新增：正文与元信息编辑功能

在阅读页支持原地编辑正文、一句话总结、摘要，并可将段落转换为不同级别的标题。

- **正文编辑**：点击「编辑」按钮进入编辑模式，正文区域变为 `contentEditable`，可直接修改文字内容
- **一句话总结 / 摘要编辑**：编辑模式下右栏对应区域均可直接编辑
- **标题级别转换**：编辑模式下工具栏提供 H1/H2/H3/H4/正文 五个按钮，将光标置于段落中点击即可转换该段落的标题级别；转换后自动重建目录
- **旧剪藏兼容**：纯文本降级内容在进入编辑模式时自动包装为 `<p>` 段落，确保可编辑
- **保存与取消**：保存时收集 contentHtml/contentText/oneliner/summary/outline 一并 PUT 到后端；取消时从快照恢复原始内容
- **ESC 键智能处理**：编辑模式下 ESC 触发取消编辑，非编辑模式下 ESC 关闭阅读页

### 技术说明（编辑功能）

- **后端**：`updateClipping()` 扩展支持 `contentHtml`/`contentText`/`outline` 字段；PUT 路由对 `contentHtml` 强制重新 `sanitizeHtml()` 清洗（XSS 防线）
- **前端**：`document.execCommand('formatBlock')` 实现标题级别转换；`extractOutlineFromDom()` 从 DOM 提取大纲与后端逻辑一致
- **安全**：用户编辑后的 HTML 经后端 `sanitizeHtml()` 白名单过滤后才入库，前端展示时使用后端返回的清洗后版本
- **状态管理**：`state.isEditing` 标记编辑态，`state.editSnapshot` 保存原始数据快照供取消恢复
